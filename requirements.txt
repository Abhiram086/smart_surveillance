# ── Server ────────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart          # required by FastAPI for UploadFile / File

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary           # PostgreSQL driver (binary = no C compiler needed on Windows)
python-dotenv             # loads backend/.env so DATABASE_URL is picked up automatically

# ── Auth ──────────────────────────────────────────────────────────────────────
passlib[bcrypt]           # password hashing
pydantic                  # request/response models used in app.py

# ── AI / Vision ───────────────────────────────────────────────────────────────
torch                     # PyTorch — GPU used automatically if CUDA is available
ultralytics               # YOLOv8 (downloads model weights on first run)
opencv-python             # frame capture, drawing, JPEG encoding
numpy

# ── Tracking / Math ───────────────────────────────────────────────────────────
scipy
shapely
filterpy
# lap removed — broken on Windows without a C compiler.
# ultralytics falls back to scipy-based matching automatically.

# ── Utilities ─────────────────────────────────────────────────────────────────
PyYAML
requests
pillow
psutil
